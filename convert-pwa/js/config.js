/* config.js — the only file you edit after deploying.
 *
 * After Railway gives you a public URL, paste it here (no trailing slash),
 * then redeploy the frontend. Leave it "" to run fully in-browser
 * (PDF/Word routes stay best-effort).
 *
 * Example: convertApi: "https://reformat-server-production.up.railway.app"
 */
window.APP_CONFIG = {
  convertApi: "reformat-production.up.railway.app",
};
